package net.zdsoft.eis.base.converter.util;import java.text.NumberFormat;public class ResourceUtils {	/**	 * 根据状态获取对应消息	 * @param status	 * @return	 */	public static String statusMessage(Integer status){		if(status == 3){			return "资源转换出错，不能播放！";		}else if(status == 2){			return null;		}else if (status == 1){			return "资源正在转换，请稍候再试！";		}else{			return "资源等待转换中，请稍候再试！";		}	}	/**	 * 获取状态	 * @param state	 * @return	 */	public static String getStatusTitle(Integer state){		switch (state) {			case 0:				return "等待转换";			case 1:				return "正在转换";			case 2:				return "转换成功";			case 3:				return "转换出错";			case 4:				return "预转换";			default:				return null;		}	}   /**     * 得到M为单位的文件大小 小于100kb的都以0.1M计     * @param size     * @return     */    public static String getSizeForM(long size){        String Msize ="";        //小于100KB        if(size/1024/100<=1){            Msize="0.1";        }else{            float m = (float)size/1024/1024;            NumberFormat format = NumberFormat.getInstance();            format.setMaximumFractionDigits(1);            Msize = format.format(m);         }        return Msize;    }}